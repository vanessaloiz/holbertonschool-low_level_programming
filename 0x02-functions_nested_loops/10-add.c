#include"main.h"
/**
 * add - Adds two integer and return the result.
 * @sum1: First number to add.
 * @sum2: Second number to add.
 * Return: 0
 */
int add(int sum1, int sum2)
{
	return (sum1 + sum2);
}
